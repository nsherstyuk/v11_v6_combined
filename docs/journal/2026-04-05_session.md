# Session Journal — 2026-04-05

**Time:** 2:00 PM – 2:10 PM ET  
**Next session should:** Start building V11 — create project structure, port v8 components, implement DarvasDetector

---

## What Happened This Session

### Phase 1: Resolved All Open Design Questions (COMPLETED)

Reviewed the 7 open questions from `docs/journal/2026-03-30_session.md` and finalized all decisions:

| # | Question | Decision |
|---|---|---|
| 1 | Instrument scope | **XAUUSD + EURUSD + USDJPY** — all three from day one |
| 2 | Darvas Box parameters | **top=15, bottom=15, width=0.3 ATR, duration=20, confirm=3** — starting values, tunable via backtest |
| 3 | LLM confidence threshold | **75** — higher than swing agent (70) due to noisier 1-min timeframe |
| 4 | Economic calendar | **Grok training knowledge initially** — external API later if insufficient. IBKR has no calendar API; options include TradingEconomics, FCS API |
| 5 | Short trades | **Both long and short** from start |
| 6 | LLM choice | **Grok 4-1 Fast** (confirmed) — designed for swappability via LLMFilter protocol |
| 7 | Project location | **`C:\ibkr_grok-_wing_agent\v11\`** (confirmed) |

### Phase 2: Updated Living Documents (COMPLETED)

- Updated `docs/V11_DESIGN.md` — added confirmed parameters table, replaced Open Questions section with Resolved Decisions table, updated status to "design finalized, ready to build"
- Updated `docs/PROJECT_STATUS.md` — updated V11 status to 🟢, added Key Decisions table, added journal reference

---

## Files Modified This Session

| File | Action | Details |
|---|---|---|
| `docs/V11_DESIGN.md` | Edited | §2: Added confirmed Darvas params table. §4.6: Confirmed Grok. §8: Replaced open questions with resolved decisions |
| `docs/PROJECT_STATUS.md` | Edited | Updated date, V11 status to 🟢, added Key Decisions table, added journal ref |
| `docs/journal/2026-04-05_session.md` | Created | This file |

---

## Key Decisions Made

| Decision | Rationale |
|---|---|
| All 3 instruments from day one | User preference — no need to phase in |
| `bottom_confirm_bars = 15` (new param) | Symmetric with top confirmation — same logic for box floor |
| `min_box_width_atr = 0.3` (down from 0.5) | Less aggressive filtering — catch more real consolidations while still filtering tick noise |
| `breakout_confirm_bars = 3` (up from 2) | Extra bar of confirmation reduces false breakouts on noisy 1-min data |
| Confidence threshold = 75 | 1-min bars produce more signals/noise than daily; higher threshold = more selective |
| Economic calendar: defer external API | Keeps V11 initial build simpler; Grok knows recurring event patterns (FOMC, NFP, CPI) |

---

## What NOT to Do

- **Don't modify v8** — reference only at `C:\nautilus0\v8_confirmed_rebreak\`
- **Don't modify the swing agent** — it may be running in paper mode
- **Don't put new files in `C:\nautilus0\`** — all new work in `C:\ibkr_grok-_wing_agent\`

---

## Ready for Next Session

All design questions are resolved. V11 is ready to build. Suggested build order:

1. Create `v11/` directory structure (per V11_DESIGN.md §6)
2. Port v8 components: `IBKRConnection`, `BarAggregator`, `ImbalanceClassifier`, `types.py`
3. Implement `DarvasDetector` (core new component)
4. Implement `LLMFilter` protocol + `GrokFilter`
5. Implement `TradeManager` (extract from v8 `run_live.py`)
6. Wire up `LiveEngine` orchestration
7. Write tests (two-phase: spec then code, per standards)
8. Backtest with grid search for Darvas parameter optimization

### Still Open (Technical — to resolve during implementation)

- **Multiple boxes** — Can multiple Darvas boxes be active simultaneously? (nested boxes)
- **Box invalidation** — What happens if price breaks below box bottom during formation? Reset or keep tracking?
