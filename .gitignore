# Environment secrets
.env

# Logs (keep directory, ignore contents)
logs/*.log
logs/*.log.*
grok_logs/

# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
.pytest_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
.windsurf/

# Grid search CSVs (large, reproducible from code)
v11_grid_*.csv

# Tick vault cache (downloaded .bi5 files, reproducible)
tick_vault_data/

# Live tick logs (large, generated at runtime)
data/ticks/

# Migrated historical data from nautilus0/data/ (315 MB total — IBKR-MID
# FX bars + Nautilus catalog parquets). Reproducible from the Dropbox
# source backup; should not be tracked in git. See data/README.md for
# provenance and how to repopulate if missing.
data/historical/
data/catalog/
data/test_catalog/

# Claude Code worktree/session state
.claude/

# V11 live runtime state (logs, trades, locks, state files)
v11/live/logs/
v11/live/state/
v11/live/trades/
v11/live/.gateway_manager.lock

# Backtest result text files (reproducible from scripts)
v11/backtest/*_results.txt
v11/backtest/output_*.txt
v11/backtest/research_output.txt
v11/backtest/intraday_results.txt

# Backtest results/cache directory (LLM response cache + JSON outputs, reproducible)
v11/backtest/results/

# Misc reference materials downloaded into repo root (not part of source)
NYMEX_DL_NG1*.csv
*.pdf
# One-off LLM transcripts dumped to repo root
grok-chat.md
gemini-conversation-*.md

# macOS metadata
.DS_Store
